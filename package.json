{
  "name": "hive-ledger",
  "version": "1.0.0",
  "description": "Hive Ledger — Unified provenance protocol for Swarm & Bee",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create hive-ledger",
    "db:migrate": "wrangler d1 execute hive-ledger --file=migrations/001_schema.sql",
    "db:migrate:local": "wrangler d1 execute hive-ledger --local --file=migrations/001_schema.sql"
  },
  "author": "Swarm & Bee LLC",
  "license": "UNLICENSED",
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
